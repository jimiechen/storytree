### **明天启动 MVP 之前，今晚必须完成「环境/数据/技术/范围」四张前置清单；MVP 范围严格收敛到“写小说 + 结构化场景 + 一键拆镜”的最小闭环，不碰图像、视频、3D、协作。** 下面分两部分：第一部分是前置检查工作任务清单（今晚做），第二部分是 PRD 大纲（明天开工就照着拆 ticket）。

---

## **第一部分：前置检查工作任务清单（今晚 / T0）**

按四个维度组织，每个 Item 都标了责任人建议、预计耗时和"必须完成"还是"可推迟"。

### **一、范围与目标对齐（最重要，30 分钟）**

```text
✅ 必做
```

| 项 | 内容 | 预计 |
|---|---|---|
| 1.1 | 写一句话定义本次 MVP：「让一个小说作者在 30 分钟内把一段章节变成可编辑的结构化场景与初版分镜镜头」 | 5min |
| 1.2 | 明确 **不在范围内** 的能力：图像生成、视频生成、3D 草稿、剧本输出、协作编辑、移动端、用户登录系统、付费 License、实时同步 | 5min |
| 1.3 | 明确 **目标用户画像**：单机使用的网文 / 短剧 / AI 视频创作者（1 人项目，无团队协作需求） | 5min |
| 1.4 | 明确 **本期成功定义**：T+14 天能录一段 2 分钟视频，演示「粘贴小说 → 看到结构化场景 → 一键生成 8 个分镜镜头 → 编辑保存」 | 5min |
| 1.5 | 明确 **本期非目标**：不做收费、不做插件市场、不接真实 LLM Provider（只用 Mock + 一条可选的真实通道做 demo） | 10min |

输出物：`docs/mvp/SCOPE-NOVEL-MVP.md`

---

### **二、环境与基础设施检查（约 1 小时）**

```text
✅ 必做
```

| 项 | 内容 | 预计 | 风险点 |
|---|---|---|---|
| 2.1 | 确认开发分支模型：`main` 稳定、`feature/novel-mvp` 长期分支、子任务起短分支合并回 feature | 10min | 不要直接在 main 上做 |
| 2.2 | 确认本地启动正常：`pnpm install` / `pnpm dev` / `localhost:3000` 三步无报错 | 10min | Bun / Node 版本一致性 |
| 2.3 | 确认 storytree workspace 结构正确（已有 `caiode/opencode-1.4.0/packages/app`），决定小说编辑器落在哪个 package | 10min | 落点直接影响后续路径 |
| 2.4 | 确认 TypeScript 编译无 error（`pnpm tsc --noEmit`） | 5min | 历史欠债清零 |
| 2.5 | 确认现有 3D Shot Draft、shot-3d-store、ThreeViewport 可以正常运行 → 表明上一轮修复已稳定，可作为兄弟模块共存 | 10min | |
| 2.6 | 确认你已有的 6 份 roadmap 文档（CLAUDE-CODE-SRC-ARCHITECTURE-BASELINE 等）已 commit，避免明天边写边参考时仍未入库 | 5min | |
| 2.7 | 准备 `.gitignore` 检查：本地数据库、临时 JSON、用户工程目录不要进仓库 | 5min | |
| 2.8 | 在 README 或 docs 增加一条「本周专注：Novel MVP」标识，避免其他模块顺手并入 | 5min | |

输出物：`docs/mvp/PRE-FLIGHT-CHECKLIST.md`（这份清单本身可以作为附件）

---

### **三、数据模型 / 存储前置（约 1.5 小时，关键路径）**

```text
✅ 必做
```

这是明天开工最容易卡壳的地方，今晚务必把数据形态拍死。

| 项 | 内容 | 预计 |
|---|---|---|
| 3.1 | 确定 NovelProject 顶层数据结构：id / title / createdAt / updatedAt / settings / chapters / characters / locations / scenes / shots | 15min |
| 3.2 | 确定章节（Chapter）结构：id / title / order / rawText / sceneIds | 10min |
| 3.3 | 确定场景（Scene）结构：id / chapterId / order / title / summary / location / characters[] / mood / goal / conflict / rawTextSpan(start,end) / shotIds | 15min |
| 3.4 | 确定镜头（Shot）结构：id / sceneId / order / shotSize / cameraAngle / cameraMovement / composition / durationSec / actionDescription / dialogue / imagePromptSeed / videoPromptSeed | 15min |
| 3.5 | 确定 Character / Location 卡的最小字段：id / name / shortDescription / appearance / tags（其它字段后期补） | 10min |
| 3.6 | 确定持久化方案：本地文件系统 JSON（推荐）vs IndexedDB vs SQLite。MVP 推荐 **本地 JSON 文件 + 单文件项目** 模式：`<project>.novelproj.json` | 15min |
| 3.7 | 确定 Schema 版本字段：`schemaVersion: 1`，并预留 migration 钩子（不写实现，先占位） | 5min |
| 3.8 | 确定 ID 生成：统一用 `nanoid` / `crypto.randomUUID`，不要用时间戳 | 5min |
| 3.9 | 编写 5 条假数据 fixture：1 个 Project + 2 个 Chapter + 4 个 Scene + 8 个 Shot + 3 个 Character + 2 个 Location，作为明天 UI 调试样本 | 15min |

输出物：`packages/novel-core/src/types.ts` 草稿 + `fixtures/sample-novel.json`

---

### **四、技术栈与架构选型（约 1 小时）**

```text
✅ 必做
```

| 项 | 内容 | 预计 | 决策 |
|---|---|---|---|
| 4.1 | 确认 UI 框架沿用 Solid.js（与 3D Shot 一致，避免双框架） | 5min | Solid |
| 4.2 | 确认编辑器：MVP 用 contentEditable + 自己控制选区，**不上 TipTap / Lexical / Slate**，不引入 ProseMirror 体系；后期再换 | 15min | 简陋版 |
| 4.3 | 确认状态管理：沿用 Solid `createStore` + signal 模式，与 shot-3d-store 风格一致 | 5min | createStore |
| 4.4 | 确认路由：Solid Router，新增 `/novel`、`/novel/:projectId`、`/novel/:projectId/chapter/:chapterId` | 10min | |
| 4.5 | 确认 LLM 通道接口形态：`TextProvider.complete(prompt, opts) → AsyncIterator<string>`，本期实现两个适配：`MockTextProvider`（固定模板回复）+ `OpenRouterTextProvider`（可选，仅用于 demo 演示） | 15min | |
| 4.6 | 确认 Skill 落点：`.claude/skills/novel-outline/SKILL.md` 与 `.claude/skills/story-to-shot/SKILL.md` 必须在 T0 前先把骨架写出来（哪怕内容粗糙），避免明天写代码时找不到 Skill 文件 | 15min | |
| 4.7 | 确认错误监控 / 日志：MVP 阶段先 `console.*` + 一个最简的 `logger.ts`，不上 Sentry | 5min | |

输出物：`docs/mvp/TECH-STACK-NOVEL-MVP.md`

---

### **五、UI / 交互前置（约 45 分钟）**

```text
⚠️ 部分必做
```

| 项 | 内容 | 预计 | 是否必做 |
|---|---|---|---|
| 5.1 | 画 3 张低保真 wireframe：项目列表页、章节编辑页（左大纲 + 中编辑器 + 右场景卡）、场景详情页（场景卡 + 镜头列表） | 20min | 必做 |
| 5.2 | 确定快捷键最少集合：Ctrl+S 保存、Ctrl+N 新场景、Ctrl+Shift+S 拆镜、Ctrl+/ 唤起 Skill | 10min | 必做 |
| 5.3 | 确定深浅主题：直接复用 3D Shot 的 theme-controller 的色值思路，不重复设计 | 5min | 推荐 |
| 5.4 | 决定首屏空状态文案：「还没有项目，从导入小说或新建项目开始」 | 5min | 推荐 |
| 5.5 | 决定加载 / 失败 / 任务运行中的统一 UI 反馈样式 | 10min | 推荐 |

输出物：`docs/mvp/WIREFRAME-NOVEL-MVP.md`（手绘也可以，关键是定主结构）

---

### **六、风险与依赖前置（30 分钟）**

```text
✅ 必做
```

| 项 | 内容 | 备注 |
|---|---|---|
| 6.1 | 确认是否需要联网 LLM：MVP 不强依赖，任何一台机器都能离线跑 Mock | 重要 |
| 6.2 | 确认 OpenRouter 密钥获取路径：单独存 `.env.local`，不要进仓库 | 安全 |
| 6.3 | 列出 Top 5 风险，每条配一个止损方案 | 见下表 |

风险表：

| 风险 | 概率 | 止损 |
|---|---|---|
| 编辑器选区与场景标记关联复杂超预期 | 中 | 退化为「整段章节作为一个场景」，后期再做精细 span |
| LLM Mock 输出结构不稳定 | 低 | 用固定 JSON 模板，不让模型生成结构 |
| 拆镜结果质量差 | 中 | MVP 阶段先承认是 Demo 级别，重点是流程跑通 |
| 本地 JSON 写文件被浏览器限制 | 中 | 用 File System Access API + 降级到下载 / 上传 |
| 时间不够 | 高 | 把 Character / Location 卡做成 stub，下一期再完善 |

---

### **七、Day 0 收尾（晚上睡前 15 分钟）**

```text
✅ 必做
```

```text
1. 把今晚产出的 6 份文档全部 commit 到 feature/novel-mvp 分支
2. 在仓库根目录创建 docs/mvp/INDEX.md 汇总所有 MVP 文档入口
3. 在你的任务管理工具里新建「Novel MVP」看板，最少四列：Backlog / Doing / Review / Done
4. 把下面 PRD 大纲拆成 epic + ticket，全部进 Backlog
5. 给自己留一句话目标：明天 EOD 前必须看到「项目列表 + 创建项目 + 打开章节空白编辑器」三件事跑通
```

---

## **第二部分：PRD 大纲（Novel Editor MVP）**

下面这份大纲直接对应明天可拆 ticket 的颗粒度，建议保存为 `docs/mvp/PRD-NOVEL-EDITOR-MVP.md`。

---

### **PRD：Novel Editor MVP**

**版本** v0.1 / **作者** Tabbit / **日期** 2026-05-16 / **状态** READY_FOR_DEV

---

#### **1. 背景与目标**

OpenCode Creative Studio 的核心入口是小说编辑器。MVP 的唯一目标是验证「文本 → 结构化场景 → 初版分镜」这一最关键的创作前置链路。本期不做图像、视频、3D、剧本、协作。

**北极星指标**：一个测试用户能在 30 分钟内，把一段 2000 字以内的小说章节，转化为可编辑、可保存、可重新打开的「结构化场景列表 + 镜头列表」，并完成至少一次 AI 辅助拆镜。

**本期成功标志**：

```text
1. 能新建 / 打开 / 保存项目
2. 能在编辑器中粘贴小说原文
3. 能手动或 AI 辅助创建场景卡
4. 能从场景一键生成 5–10 个镜头
5. 能编辑镜头字段并保存
6. 关闭重启后数据不丢
```

---

#### **2. 用户角色**

```text
单机创作者
  - 网文 / 短剧 / 漫画 / AI 视频创作者
  - 一人项目，不需要协作
  - 已有写作习惯，会复制粘贴自己的稿子
  - 期待 AI 不替他写，而是帮他做"分场 / 分镜 / 整理"
```

不在本期服务的角色：团队、编辑、出版方、付费用户体系下的高级用户。

---

#### **3. 核心用户故事**

```text
US1：作为创作者，我要新建一个小说项目并命名，避免和其它项目混在一起。
US2：作为创作者，我要把一章小说粘贴到编辑器里，能像写文档一样修改。
US3：作为创作者，我要把章节切分成若干场景，每个场景能独立查看与编辑。
US4：作为创作者，我要为每个场景填写发生地点、出场角色、目标和冲突。
US5：作为创作者，我要让 AI 帮我把当前场景拆成 5–10 个镜头，并能逐条修改。
US6：作为创作者，我要给每个镜头补充景别、机位、镜头运动和动作描述。
US7：作为创作者，我要随时保存项目，关电脑后下次打开内容还在。
US8：作为创作者，我要看到 AI 任务正在运行 / 完成 / 失败，并可重试或取消。
US9：作为创作者，我要能切换深 / 浅主题，长时间写作不刺眼。
US10：作为创作者，我要能在不联网的情况下使用 Mock AI，先把流程跑通。
```

---

#### **4. 功能范围**

##### **4.1 必做（P0）**

```text
F1  项目管理：新建项目、项目列表、打开项目、删除项目（带二次确认）
F2  章节管理：新建章节、章节列表（左侧大纲）、重命名、删除、排序（拖拽）
F3  章节编辑器：纯文本编辑、自动保存（节流 1s）、字数统计
F4  场景结构化：手动新建场景卡、绑定章节、填写 title/summary/location/characters/goal/conflict
F5  AI 拆场景：选中一段章节文字 → 调用 novel-outline Skill → 返回场景卡草稿
F6  AI 拆镜头：在场景卡上点「生成镜头」 → 调用 story-to-shot Skill → 返回 5–10 个 Shot
F7  镜头编辑：编辑 Shot 的所有字段；增加 / 删除 / 排序
F8  本地持久化：单文件 .novelproj.json，使用 File System Access API
F9  任务中心：右下角任务面板，显示 AI 任务状态（pending / running / done / failed）
F10 Mock AI Provider：固定模板返回结构化结果，保证流程能跑
F11 主题切换：深 / 浅
F12 错误兜底：AI 失败时给清晰提示并支持重试
```

##### **4.2 推荐（P1，时间允许才做）**

```text
F13 Character / Location 卡片管理：基础 CRUD
F14 文本选区与场景的位置绑定（rawTextSpan），可在编辑器中高亮
F15 OpenRouter 真 Provider：仅用于 demo
F16 项目导入 / 导出（.novelproj.json）
F17 最近打开项目列表
```

##### **4.3 不做（P2，明确划掉）**

```text
N1 图像 / 视频 / 3D / 剧本输出
N2 用户登录、云同步、多人协作
N3 付费 License、插件市场
N4 富文本编辑器（不上 TipTap / Lexical）
N5 移动端适配
N6 全文检索、全局替换
N7 版本历史、撤销栈持久化
```

---

#### **5. 数据模型**

```ts
// packages/novel-core/src/types.ts

export interface NovelProject {
  schemaVersion: 1;
  id: string;
  title: string;
  createdAt: string;
  updatedAt: string;
  settings: { theme: 'light' | 'dark' };

  chapters: Chapter[];
  scenes: Scene[];
  shots: Shot[];
  characters: Character[];
  locations: Location[];
}

export interface Chapter {
  id: string;
  order: number;
  title: string;
  rawText: string;
}

export interface Scene {
  id: string;
  chapterId: string;
  order: number;
  title: string;
  summary: string;
  locationId?: string;
  characterIds: string[];
  goal?: string;
  conflict?: string;
  mood?: string;
  rawTextSpan?: { start: number; end: number };
}

export interface Shot {
  id: string;
  sceneId: string;
  order: number;
  shotSize?: 'ECU' | 'CU' | 'MS' | 'WS' | 'EWS';
  cameraAngle?: 'eye' | 'high' | 'low' | 'dutch' | 'topdown';
  cameraMovement?: 'static' | 'pan' | 'tilt' | 'dolly' | 'tracking' | 'zoom';
  composition?: string;
  durationSec?: number;
  actionDescription?: string;
  dialogue?: string;
  imagePromptSeed?: string;
  videoPromptSeed?: string;
}

export interface Character {
  id: string;
  name: string;
  shortDescription?: string;
  appearance?: string;
  tags: string[];
}

export interface Location {
  id: string;
  name: string;
  shortDescription?: string;
  tags: string[];
}
```

---

#### **6. 信息架构与页面结构**

```text
/novel                      项目列表页
  ├── + 新建项目
  ├── 最近打开
  └── 项目卡片（点击进入）

/novel/:projectId           项目主界面
  ├── 顶栏：项目名 / 主题切换 / 保存状态 / 任务图标
  ├── 左栏：章节大纲 + 角色 / 地点 标签
  ├── 中栏：当前章节文本编辑器
  ├── 右栏：当前章节场景卡列表
  └── 右下：任务面板（折叠）

/novel/:projectId/scene/:sceneId   场景详情页（可弹层或独立页）
  ├── 场景元数据编辑（title/summary/location/characters/goal/conflict）
  ├── 场景对应原文段落（高亮）
  └── 镜头列表（卡片形态，可拖拽排序）
```

---

#### **7. 关键交互流**

##### **7.1 新建项目流**

```text
点击"新建项目" → 弹窗输入项目名 → 选择本地保存位置（File System Access API）
→ 创建空 .novelproj.json → 自动跳转到项目主界面
```

##### **7.2 写小说流**

```text
打开项目 → 选择章节（或新建）→ 在中栏编辑器粘贴 / 输入正文
→ 编辑器节流 1s 自动保存到内存 store → store 节流 3s 写入磁盘文件
→ 顶栏显示"已保存"或"保存中"
```

##### **7.3 AI 拆场景流**

```text
在编辑器中选中一段文字（或不选 = 整章）→ 右键或 Ctrl+/ → 选择"拆为场景卡"
→ 任务面板新增任务 task: scene.extract → 调用 novel-outline Skill + Mock Provider
→ 返回 1–N 个场景草稿 → 弹出预览面板让用户确认 → 确认后写入 scenes
→ 任务变为 done
```

##### **7.4 AI 拆镜头流**

```text
进入场景详情 → 点击"生成镜头" → 任务 task: shot.extract → story-to-shot Skill
→ 返回 5–10 个 Shot 草稿 → 直接 append 到该场景的 shots（不弹预览）
→ 用户在镜头卡片上逐个编辑
```

##### **7.5 任务失败重试流**

```text
任务面板每条任务三态：running / done / failed
失败 → 显示错误摘要 + "重试"按钮 → 重试时复用同一 task input
```

---

#### **8. 非功能需求**

```text
NFR1 性能：粘贴 5 万字章节，编辑器输入延迟 < 50ms
NFR2 自动保存：编辑停顿 3s 内必须落盘
NFR3 启动：从命令行启动到打开最近项目 ≤ 3s
NFR4 容灾：保存失败必须有用户可见提示，不能静默丢数据
NFR5 可观察：所有 AI 任务必须有 task.id / 起止时间 / 输入哈希 / 输出长度
NFR6 国际化：本期仅中文 UI，但所有文案集中在 i18n.ts，便于后续替换
NFR7 隐私：本期所有数据仅落本地磁盘，不上传任何外部服务（Mock Provider 不联网）
```

---

#### **9. AI 任务与 Skill 设计**

##### **9.1 涉及的 Skill**

```text
.claude/skills/novel-outline/SKILL.md
  作用：把一段小说文本拆成场景卡（title / summary / location / characters / goal / conflict）
  输入：rawText, optional charactersHint, optional locationsHint
  输出：Scene[]（不含 id / order，由前端补）

.claude/skills/story-to-shot/SKILL.md
  作用：把一个场景拆成 5–10 个 Shot
  输入：Scene + 上下文章节段落
  输出：Shot[]（不含 id / order）
```

##### **9.2 Task 类型**

```text
task: scene.extract
task: shot.extract
task: scene.refine（可选 P1）
task: shot.refine（可选 P1）
```

##### **9.3 Provider**

```text
MockTextProvider：本期默认，离线可用
OpenRouterTextProvider：可选，仅用于 demo 录屏
```

---

#### **10. 验收标准（Definition of Done）**

```text
DoD1 本地全新环境从 git clone 到看到项目列表页 ≤ 5 分钟
DoD2 创建项目 → 写章节 → 拆场景 → 拆镜头 → 关闭重启 → 数据完整 全流程跑通
DoD3 AI 任务在 Mock 模式下 100% 成功，在 OpenRouter 模式下能跑成功 1 次
DoD4 主题切换无残留样式
DoD5 至少 1 名外部测试用户跑完一遍并写出反馈
DoD6 错误路径有兜底：保存失败、Skill 失败、文件被占用
DoD7 docs/mvp 下所有文档同步更新，ROADMAP-PLUGIN-FIRST 中"Novel Editor Core"状态从 planned → in-progress
```

---

#### **11. 里程碑（建议两周）**

```text
Day 1   项目脚手架 + 路由 + 项目列表 + 新建项目 + 本地存储读写
Day 2   章节大纲 + 简易编辑器 + 自动保存
Day 3   场景卡数据结构 + 手动新建 + 列表展示
Day 4   场景详情页 + 镜头数据结构 + 手动新建 / 编辑
Day 5   任务中心 UI + Mock TextProvider + Skill Loader 最小版
Day 6   novel-outline Skill 接入 + scene.extract 任务 + 预览确认
Day 7   story-to-shot Skill 接入 + shot.extract 任务
Day 8   主题切换 + 文案润色 + 空状态 / 错误态
Day 9   导入导出 + 最近项目
Day 10  自测 + Bug 修复 + 拉外部用户试用
Day 11  根据反馈修最关键 3 个问题
Day 12  录制 2 分钟 demo 视频
Day 13  Buffer 日（用于追赶 / 修 bug）
Day 14  收尾、写后记、规划下一阶段
```

---

#### **12. 风险登记**

```text
R1 编辑器选区与 rawTextSpan 同步复杂 → 降级为整段绑定
R2 大文本性能 → 早期用 contentEditable 即可，必要时切换到虚拟滚动
R3 LLM 输出格式不稳 → Mock 返回固定 JSON；真实 Provider 用 JSON Schema 强约束
R4 File System Access API 浏览器兼容性 → 不支持时降级为下载 / 上传
R5 时间不够 → P1 全部砍掉，确保 P0 闭环
```

---

#### **13. 不解决的问题清单（明确转交后续）**

```text
- 富文本格式（粗体、引用、注释）
- 多版本草稿管理
- 角色 / 地点的视觉资产
- 跨章节一致性检查
- 多人协作
- 任何形式的 LLM Token 计费
```

---

### **第三部分：明天 9:00 开工的「第一小时清单」**

把这一段也复制到 `docs/mvp/DAY1-FIRST-HOUR.md`，明天早上不用想直接开工。

```text
09:00 切到 feature/novel-mvp 分支，pull 最新代码
09:05 创建 packages/novel-core/，初始化 package.json + tsconfig
09:15 把昨晚的 types.ts 落地到 packages/novel-core/src/types.ts
09:25 创建 packages/app/src/novel/ 目录，注册路由 /novel
09:35 实现项目列表页空壳（先用假数据，不接存储）
09:55 提交第一个 commit："feat(novel): scaffold novel package and route"
10:00 短暂休息，回顾今早进度，进入"新建项目"功能开发
```

---

### **最后一句**

今晚最重要的不是把代码写完，而是把 **"明天我到底要干什么 / 不干什么"** 钉死。MVP 最大的敌人是中途冒出来的"顺手做一下"。把范围清单和不做清单都写进文档，明天遇到任何犹豫就翻这两份清单做决定。等 14 天后那段 2 分钟 demo 视频录出来，整个项目的可信度会比现在高一个数量级。

*内容由 AI 生成仅供参考*