下面是可直接粘贴给 Trae / SOLO Code 的 **Mock 模式重新执行 Week 0** 提示词。它的目标是：**重新检查、修订并生成 Week 0 文档，但不进入真实业务开发，不接真实 Agent，不调用真实模型，不修改小说编辑器业务源码。**

```markdown
# 任务：Mock 模式重新执行 Week 0 可行性验证

## 一、任务背景

当前项目目录：

```text
/workspace/caiode
```

项目目标是在 `opencode v1.4.0` 基础上二次开发“卡牌物语 AI 小说编辑器”。

当前已有 Week 0 初版报告：

```text
docs/planning/week-0-feasibility-report.md
```

或可能存在：

```text
docs/planning/source-analysis/week-0-feasibility-report.md
docs/planning/week0/
```

请根据仓库实际情况查找。

当前已知问题：

1. 现有 Week 0 报告中把结论写成了 `Go` 或“完全可行”。
2. 但实际环境中 `Bun` 尚未安装。
3. 因此依赖安装、编译构建、基础运行尚未完成闭环验证。
4. 当前更准确的结论应为：

```text
Conditional Go
```

也就是：

```text
opencode v1.4.0 源码已经就位，源码结构初步可识别，AI 小说编辑器二开路线初步可行；
但 Bun 未安装，依赖安装、编译构建和基础运行尚未完成闭环验证；
因此不能直接进入 Week 1 业务开发，必须先完成 Week 0 后续验证。
```

## 二、执行模式：Mock 模式

本任务必须以 **Mock 模式** 执行。

Mock 模式含义：

1. 可以读取文件。
2. 可以检查目录结构。
3. 可以检查 Git 状态。
4. 可以检查 package.json、workspace、scripts、lockfile。
5. 可以输出分析文档。
6. 可以修订 Week 0 报告结论。
7. 可以设计 FakeProvider / Mock Data / FakeAgentProvider 的验证路线。
8. 不允许接真实 Agent。
9. 不允许调用真实模型。
10. 不允许执行真实远程 API。
11. 不允许生成小说编辑器业务代码。
12. 不允许大范围重构。
13. 不允许修改 opencode 上游源码。
14. 不允许提交 node_modules、临时目录、缓存文件。
15. 不允许把“未实际验证”的内容写成“已完成”。

本次重新执行的目标不是开发功能，而是重新校准 Week 0 文档和 Go/No-Go 判断。

## 三、核心目标

请重新执行 Week 0 可行性验证，完成以下目标：

1. 检查当前仓库状态。
2. 检查 `opencode-1.4.0` 是否存在。
3. 检查 `opencode-1.4.0` 的工程结构。
4. 检查当前开发环境工具链。
5. 修订原 Week 0 报告中偏乐观的结论。
6. 将最终判断调整为 `Conditional Go`，除非你实际完成了 Bun 安装、依赖安装、构建和运行验证。
7. 生成或更新 Week 0 四份正式检查文档。
8. 输出最终执行报告。

## 四、禁止事项

请严格遵守：

```text
不要修改小说编辑器业务源码
不要生成业务 UI 代码
不要生成 Provider 实现代码
不要接真实 Agent
不要执行真实模型调用
不要执行真实远程 API
不要启动长期后台服务
不要修改 opencode-1.4.0 上游源码
不要提交 node_modules
不要提交临时目录
不要提交缓存文件
不要提交非预期 lockfile 修改
不要把未验证项写成已完成
不要把 Conditional Go 写成完全 Go
不要扩展 PRD 范围
不要做大范围重构
```

## 五、第一步：检查 Git 与目录状态

请在 `/workspace/caiode` 下执行：

```bash
pwd
git branch --show-current
git status
git log --oneline -10
ls
find . -maxdepth 2 -type d | sort | head -100
find docs/planning -maxdepth 4 -type f | sort
find . -maxdepth 3 -type d -name "opencode-1.4.0"
```

请记录：

- 当前分支
- 当前工作区是否干净
- 最新 10 条提交
- `opencode-1.4.0` 是否存在
- `docs/planning/week0/` 是否存在
- 旧版 `week-0-feasibility-report.md` 的实际路径

## 六、第二步：检查 opencode v1.4.0 工程结构

如果存在：

```text
opencode-1.4.0/
```

请执行：

```bash
ls opencode-1.4.0
find opencode-1.4.0 -maxdepth 2 -type f | sort | head -100
find opencode-1.4.0 -maxdepth 3 -type d | sort | head -150
test -f opencode-1.4.0/package.json && cat opencode-1.4.0/package.json
test -f opencode-1.4.0/bun.lock && echo "bun.lock exists"
test -f opencode-1.4.0/bunfig.toml && cat opencode-1.4.0/bunfig.toml
test -f opencode-1.4.0/turbo.json && cat opencode-1.4.0/turbo.json
test -f opencode-1.4.0/tsconfig.json && cat opencode-1.4.0/tsconfig.json
```

请判断：

- 是否存在根 `package.json`
- 是否存在 `bun.lock`
- 是否存在 `bunfig.toml`
- 是否存在 `turbo.json`
- 是否存在 `tsconfig.json`
- 是否存在 `packages/`
- 是否能判断为 monorepo
- 是否使用 Bun
- 是否存在 workspace 配置
- 是否存在 build / dev / test / typecheck / lint 脚本
- 是否存在 postinstall / prepare / patch 等脚本
- 是否存在 app / cli / server / sdk / ui 等模块

如果 `opencode-1.4.0` 不存在，请不要猜测，直接写明：

```text
opencode-1.4.0 当前未在仓库中找到，Week 0 不能继续做源码结构验证。
```

## 七、第三步：检查本地工具链

请执行以下命令，允许某些命令失败，但必须记录结果：

```bash
node --version
npm --version
bun --version
pnpm --version
yarn --version
git --version
rustc --version
cargo --version
go version
python3 --version
make --version
gcc --version
clang --version
```

请输出工具链检查表：

| 工具 | 当前状态 | 版本 | 是否必需 | 备注 |
|---|---|---|---|---|

特别注意：

- 如果 Bun 未安装，必须明确写：

```text
Bun 未安装，因此不能判断依赖安装、编译构建和基础运行已经完成。
```

- 如果 Node.js 版本较新，例如 v25.x，必须写：

```text
Node.js 已安装，但版本较新，后续依赖安装或构建失败时需要排查是否存在非 LTS 兼容性问题。
```

## 八、第四步：修订 Week 0 初版报告

请查找并修订现有 Week 0 初版报告，可能路径包括：

```text
docs/planning/week-0-feasibility-report.md
docs/planning/source-analysis/week-0-feasibility-report.md
docs/planning/week0/week-0-feasibility-report.md
```

如果找不到旧报告，请新建：

```text
docs/planning/week0/week-0-feasibility-report.md
```

修订要求：

### 1. 将核心结论从 Go 改为 Conditional Go

请将类似以下表述：

```text
opencode v1.4.0 在当前开发环境下可以完成依赖安装、编译、基础运行和源码结构识别
```

改为：

```text
opencode v1.4.0 源码结构已完成初步识别，当前环境已具备部分基础工具链条件，但 Bun 未安装，依赖安装、编译构建和基础运行尚未完成闭环验证。因此当前 Week 0 判断应为 Conditional Go。
```

### 2. 修订总体评估表

请使用类似以下评估口径：

```markdown
| 评估维度 | 当前状态 | 风险等级 | 备注 |
|---|---|---|---|
| 源码就位 | ✅ 已完成 / ⚠️ 待确认 | 低 / 中 | 根据实际目录检查填写 |
| 源码识别 | ✅ 初步完成 | 低 | 已识别 core/app/sdk/ui 等关键模块，按实际情况填写 |
| 依赖安装 | ⚠️ 待验证 | 中 | Bun 未安装或尚未执行依赖安装 |
| 编译构建 | ⚠️ 待验证 | 中 | 尚未执行最小 typecheck/build |
| 基础运行 | ⚠️ 待验证 | 中 | 已识别候选入口，但尚未启动验证 |
| 二开接入 | ✅ 初步可行 | 中 | 推荐继续优先评估 workspace mode，其次新页面/panel |
| 开发路线 | ✅ 初步可行 | 低 | Mock + Provider + FakeAgentProvider + TDD 路线合理 |
```

### 3. 明确区分“已验证”和“待验证”

请全文检查并修正以下类型的表达：

错误表达：

```text
依赖安装可行
编译构建可行
基础运行可行
多种启动模式已可运行
完善的插件系统和扩展点
```

如果没有实际执行成功，请改为：

```text
依赖安装待验证
编译构建待验证
基础运行待验证
已识别候选运行入口，但尚未完成启动验证
存在 plugin/provider/tool/session 等可借鉴结构，但接入方式仍需进一步验证
```

### 4. 增加 Week 1 前置条件

请在报告中增加：

```markdown
## Week 1 前置条件

进入 Week 1 业务开发前，必须完成：

1. 安装 Bun，并记录版本。
2. 确认 `opencode-1.4.0/package.json` 的 packageManager / workspaces / scripts。
3. 执行依赖安装验证，优先使用 lockfile 对应命令。
4. 执行最小 typecheck 或 build。
5. 短时间验证 CLI 或 app 启动入口。
6. 明确小说编辑器接入方式优先级：
   - workspace mode
   - 新页面
   - panel
   - 插件
   - 独立 app
7. 输出正式 Go/No-Go Checklist。
```

## 九、第五步：生成 Week 0 四份正式文档

请创建目录：

```text
docs/planning/week0/
```

并生成或更新以下四份文件：

```text
docs/planning/week0/WEEK0-OPENCODE-1.4.0-DEPENDENCY-CHECK.md
docs/planning/week0/WEEK0-OPENCODE-1.4.0-BUILD-RUN-CHECK.md
docs/planning/week0/WEEK0-NOVEL-EDITOR-FEASIBILITY-CHECK.md
docs/planning/week0/WEEK0-GO-NOGO-CHECKLIST.md
```

### 文档 1：WEEK0-OPENCODE-1.4.0-DEPENDENCY-CHECK.md

必须包含：

```markdown
# Week 0：opencode v1.4.0 依赖检查报告

## 1. 检查结论

当前结论必须基于实际检查结果。

如果 Bun 未安装，请写：

**结论：Conditional Go。**

当前已完成源码与工程文件初步识别，但 Bun 未安装，依赖安装尚未完成闭环验证。

## 2. 仓库与目录状态

- 当前分支：
- 工作区状态：
- opencode-1.4.0 是否存在：
- 根 package.json 是否存在：
- bun.lock 是否存在：
- bunfig.toml 是否存在：
- turbo.json 是否存在：
- tsconfig.json 是否存在：

## 3. 工具链检查结果

使用表格列出 node / npm / bun / pnpm / yarn / git / rustc / cargo / go / python3 / make / gcc / clang。

## 4. 包管理器判断

说明是否使用 Bun，依据是什么。

## 5. workspace / monorepo 判断

说明是否是 monorepo，依据是什么。

## 6. scripts 摘要

列出 package.json 中的主要 scripts。

## 7. install 风险

说明是否可以执行 install，是否依赖网络，是否可能修改 lockfile，是否可能受国内网络影响。

## 8. 当前阻塞点

重点写 Bun 未安装、依赖安装未执行等真实状态。

## 9. 下一步建议

建议安装 Bun 后执行最小依赖安装验证。
```

### 文档 2：WEEK0-OPENCODE-1.4.0-BUILD-RUN-CHECK.md

必须包含：

```markdown
# Week 0：opencode v1.4.0 构建与运行检查报告

## 1. 检查结论

如果未完成构建和运行，请写：

**结论：待验证。**

当前尚未完成依赖安装，因此不能判断构建和运行已经成功。

## 2. 候选构建命令

从 package.json scripts 中提取 build / typecheck / test / lint 等命令。

## 3. 候选运行入口

识别 CLI / Web App / Server / SDK / UI / Desktop 等候选入口。

## 4. 已执行命令

列出实际执行过的命令。

如果没有执行 build，请明确写：

```text
尚未执行构建命令。
```

如果没有启动应用，请明确写：

```text
尚未执行基础运行验证。
```

## 5. 构建风险

说明 Bun、Node 版本、native dependency、网络、平台兼容性等风险。

## 6. 最小验证计划

建议顺序：

1. 安装 Bun。
2. 执行版本确认。
3. 执行依赖安装。
4. 执行最小 typecheck。
5. 执行最小 build。
6. 短时启动候选入口。
7. 停止服务，不保留长期后台进程。
```

### 文档 3：WEEK0-NOVEL-EDITOR-FEASIBILITY-CHECK.md

必须包含：

```markdown
# Week 0：AI 小说编辑器二开可行性检查报告

## 1. 检查结论

建议结论：

**初步可行，但依赖 opencode 基础构建验证完成后才能进入 Week 1。**

## 2. 推荐接入方式

按优先级说明：

1. workspace mode
2. 新页面
3. panel
4. 插件
5. 独立 app

需要说明为什么 workspace mode 优先。

## 3. Mock 模式开发路线

说明 Week 1 应继续使用：

- Mock 数据
- Provider 抽象
- FakeAgentProvider
- TDD
- 不接真实 Agent
- 不调用真实模型

## 4. 最小业务对象

只保留以下对象，不扩展范围：

- Project
- Sandbox
- Chapter
- Character
- AITask
- AILog

## 5. Provider 抽象草案

只写职责和方法名，不写实现代码：

- NovelProjectProvider
- NovelChapterProvider
- NovelCharacterProvider
- NovelAgentProvider
- NovelSyncProvider

## 6. FakeAgentProvider 模拟行为

至少包含：

- AI 续写成功
- AI 改写成功
- AI 摘要成功
- AI 任务失败
- AI 任务取消
- 权限被拒绝
- 配额不足，仅做状态模拟

## 7. AgentBridge 安全边界

允许工具：

- Read
- Edit
- Write
- Glob
- Grep
- 小说专用工具

禁用工具：

- Bash
- WebFetch
- WebSearch
- Agent
- Task
- 任意命令执行
- 读取环境变量
- 访问系统目录
- 访问沙箱外路径

## 8. 风险点

说明真实 Agent 接入、权限边界、沙箱限制、数据结构演进等风险。

## 9. Week 1 建议

只有在 Week 0 依赖和构建验证完成后，才进入 Week 1 Mock 数据和 Provider 接口设计。
```

### 文档 4：WEEK0-GO-NOGO-CHECKLIST.md

必须包含：

```markdown
# Week 0：Go / No-Go 检查清单

## 1. 当前判断

**Conditional Go**

## 2. Go 条件

只有满足以下条件后，才能改为 Go：

- opencode-1.4.0 目录确认存在
- Bun 已安装
- 依赖安装完成
- 最小 typecheck 或 build 通过
- 至少一个运行入口完成短时启动验证
- workspace / 页面 / panel 接入点完成初步识别
- Mock + Provider + FakeAgentProvider 路线无架构阻塞
- Git 工作区干净
- 未提交临时文件、node_modules、缓存文件

## 3. 当前已满足项

根据实际结果填写。

## 4. 当前未满足项

如果 Bun 未安装，必须列入未满足项。

## 5. 风险项

包括：

- Bun 未安装
- Node 版本较新
- 依赖下载可能受网络影响
- 构建脚本尚未验证
- 运行入口尚未启动
- 接入点仍需进一步定位
- 真实 Agent 权限边界未实现

## 6. No-Go 条件

如果出现以下情况，应判断为 No-Go：

- opencode-1.4.0 目录不存在
- package.json 缺失
- 关键依赖文件缺失且无法恢复
- 依赖安装无法完成且无替代方案
- 最小构建无法通过且无法定位原因
- 无法识别任何可用运行入口
- 小说编辑器接入需要大规模破坏性改造

## 7. 进入 Week 1 的前置动作

列出进入 Week 1 前必须完成的动作。
```

## 十、第六步：不要擅自执行高风险操作

除非明确确认安全，否则不要执行：

```bash
bun install
npm install
pnpm install
yarn install
bun run build
npm run build
pnpm build
yarn build
```

如果你判断可以执行依赖安装，也必须先说明：

1. 使用哪个包管理器。
2. 为什么使用它。
3. 是否会修改 lockfile。
4. 是否会产生 node_modules。
5. 是否会访问网络。
6. 是否需要用户确认。

本次任务默认以 Mock 文档验证为主，不强制执行真实安装和构建。

如果你没有执行安装或构建，请不要把结论写成安装成功或构建成功。

## 十一、第七步：最终 Git 检查

任务结束前执行：

```bash
git status
git diff --stat
find . -maxdepth 3 -type d -name "node_modules"
find . -maxdepth 3 -type d -name ".tmp"
find . -maxdepth 3 -type d -name "dist"
find . -maxdepth 3 -type d -name ".cache"
```

请确认：

- 是否只修改了文档
- 是否生成了临时目录
- 是否生成了 node_modules
- 是否修改了 lockfile
- 是否修改了业务源码
- 是否修改了 opencode 上游源码

如果发现异常文件，不要提交，先报告。

## 十二、最终回复格式

完成后请按以下格式回复：

```text
已完成 Mock 模式 Week 0 重新执行。

一、核心结论
- 当前判断：Conditional Go / Go / No-Go
- 判断理由：

二、已生成或更新的文件
- docs/planning/week0/WEEK0-OPENCODE-1.4.0-DEPENDENCY-CHECK.md
- docs/planning/week0/WEEK0-OPENCODE-1.4.0-BUILD-RUN-CHECK.md
- docs/planning/week0/WEEK0-NOVEL-EDITOR-FEASIBILITY-CHECK.md
- docs/planning/week0/WEEK0-GO-NOGO-CHECKLIST.md
- 如有修订：docs/planning/week-0-feasibility-report.md

三、当前已验证事项
- opencode-1.4.0 是否存在：
- package.json 是否存在：
- bun.lock 是否存在：
- workspace 是否识别：
- scripts 是否识别：
- 工具链是否检查：

四、当前未验证事项
- Bun 是否安装：
- 依赖安装是否完成：
- 构建是否完成：
- 运行入口是否启动：

五、风险与阻塞
- 

六、Git 状态
- 当前分支：
- 工作区是否干净：
- 是否存在 node_modules：
- 是否存在临时目录：
- 是否存在非预期 lockfile 修改：
- 是否存在业务源码修改：

七、下一步建议
- 
```

## 十三、特别强调

本次任务不是 Week 1 开发。

本次任务只允许做：

```text
检查
修订
文档化
Conditional Go 判断
Mock 路线确认
```

不要做：

```text
业务实现
真实 Agent 接入
真实模型调用
真实远程 API 调用
大范围重构
自动提交临时文件
```
```

### **我对这版提示词做了什么调整**

我把提示词改成了“Mock 文档验证优先”的执行方式，避免 Trae 直接进入安装、构建或业务开发。因为你当前的核心问题不是“马上写代码”，而是原 Week 0 报告把未验证事项写得过于乐观，所以这版提示词会强制 Trae 先检查 Git、目录、工具链、`package.json` 和 `opencode-1.4.0` 结构，再把结论改为 `Conditional Go`。

同时，我把输出拆成四份正式 Week 0 文档，分别对应依赖检查、构建运行检查、小说编辑器二开可行性、Go/No-Go 清单。这样后面进入 Week 1 时，不会再把“源码已就位”误当成“依赖和构建已通过”。

这版也特别加了“未执行就不能写已完成”的约束，防止 Trae 在 Bun 未安装、构建未跑通的情况下继续输出“Go”。

*内容由 AI 生成仅供参考*