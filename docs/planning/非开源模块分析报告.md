# 非开源模块分析报告

**生成日期**: 2026-04-07  
**项目**: Caiode (基于 Claude Code)

---

## 摘要

本报告详细分析了 Caiode 项目中使用的非开源依赖包及其在代码库中的使用情况。通过全面的代码搜索，我们识别出了 2 个未在 package.json 中声明但在代码中被引用的私有依赖包，以及 1 个已声明的开源 SDK。

---

## 依赖包分类

### 1. 已声明的开源依赖

| 包名 | 版本 | 许可证 | 说明 |
|------|------|--------|------|
| `@anthropic-ai/sdk` | ^0.21.1 | MIT | Claude API 客户端 SDK（已开源） |

**状态**: ✅ 开源，无需替换

---

### 2. 未声明的私有依赖（关键发现）

#### 2.1 @anthropic-ai/sandbox-runtime

**状态**: 🔴 非开源，核心运行时未公开

**引用位置**:
- [sandbox-adapter.ts](file:///workspace/caiode/claude-code-src/utils/sandbox/sandbox-adapter.ts#L2-L22)
- [powershellProvider.ts](file:///workspace/caiode/claude-code-src/utils/shell/powershellProvider.ts#L19)
- [SandboxDependenciesTab.tsx](file:///workspace/caiode/claude-code-src/components/sandbox/SandboxDependenciesTab.tsx#L85)

**导入的类型/功能**:
```typescript
import type {
  FsReadRestrictionConfig,
  FsWriteRestrictionConfig,
  IgnoreViolationsConfig,
  NetworkHostPattern,
  NetworkRestrictionConfig,
  SandboxAskCallback,
  SandboxDependencyCheck,
  SandboxRuntimeConfig,
  SandboxViolationEvent,
} from '@anthropic-ai/sandbox-runtime'

import {
  SandboxManager as BaseSandboxManager,
  SandboxRuntimeConfigSchema,
  SandboxViolationStore,
} from '@anthropic-ai/sandbox-runtime'
```

**核心功能**:
- 沙箱隔离系统（文件系统、网络、进程隔离）
- 权限限制和违规检测
- 依赖检查（bubblewrap、socat、seccomp）
- macOS/Linux/WSL2 平台支持

**package.json 声明**: ❌ 未声明

**替代方案**:
- ✅ Git Worktree（已在代码库中实现完整替代方案）
- ✅ InProcessBackend（进程内隔离）
- ✅ Tmux 终端隔离

---

#### 2.2 @anthropic-ai/mcpb

**状态**: 🟡 需确认，可能为内部包

**引用位置**:
- [plugins/schemas.ts](file:///workspace/caiode/claude-code-src/utils/plugins/schemas.ts#L578)
- [plugins/mcpbHandler.ts](file:///workspace/caiode/claude-code-src/utils/plugins/mcpbHandler.ts#L4)
- [dxt/helpers.ts](file:///workspace/caiode/claude-code-src/utils/dxt/helpers.ts#L1)

**导入的类型/功能**:
```typescript
import type { McpbManifest } from '@anthropic-ai/mcpb'
import { getMcpConfigForManifest } from '@anthropic-ai/mcpb'
import { McpbManifestSchema } from '@anthropic-ai/mcpb'
```

**核心功能**:
- MCP (Model Context Protocol) 桥接
- 插件清单验证
- MCP 配置解析

**package.json 声明**: ❌ 未声明

**说明**: 代码中使用了懒加载方式导入，以减少初始加载体积：
```typescript
// Lazy import: @anthropic-ai/mcpb barrel pulls in zod v3 schemas (~700KB of
// code that's only needed for plugin validation, not core operation)
const { getMcpConfigForManifest } = await import('@anthropic-ai/mcpb')
```

---

## 依赖使用统计

### @anthropic-ai/sandbox-runtime 使用统计

| 文件 | 行数 | 关键功能 |
|------|------|----------|
| sandbox-adapter.ts | 985 行 | 沙箱管理器适配器、配置转换、权限管理 |
| powershellProvider.ts | 1 行引用 | shellquote 兼容性注释 |
| SandboxDependenciesTab.tsx | 1 行引用 | 安装提示 |

**总计**: 约 987 行代码直接依赖

---

### @anthropic-ai/mcpb 使用统计

| 文件 | 行数 | 关键功能 |
|------|------|----------|
| plugins/schemas.ts | 1 行引用 | 类型兼容性说明 |
| plugins/mcpbHandler.ts | 420 行 | MCP 插件处理、清单验证 |
| dxt/helpers.ts | 20 行 | DXT 辅助工具、懒加载 |

**总计**: 约 441 行代码直接依赖

---

### @anthropic-ai/sdk 使用统计（已开源）

**引用位置**:
- [tokens.ts](file:///workspace/caiode/claude-code-src/utils/tokens.ts#L1)
- [toolSchemaCache.ts](file:///workspace/caiode/claude-code-src/utils/toolSchemaCache.ts#L1)
- [toolResultStorage.ts](file:///workspace/caiode/claude-code-src/utils/toolResultStorage.ts#L5)
- [ultraplan/ccrSession.ts](file:///workspace/caiode/claude-code-src/utils/ultraplan/ccrSession.ts#L10)
- [swarm/inProcessRunner.ts](file:///workspace/caiode/claude-code-src/utils/swarm/inProcessRunner.ts#L13)
- [sideQuery.ts](file:///workspace/caiode/claude-code-src/utils/sideQuery.ts#L1-L2)
- [queryHelpers.ts](file:///workspace/caiode/claude-code-src/utils/queryHelpers.ts#L1)
- [processUserInput/processUserInput.ts](file:///workspace/caiode/claude-code-src/utils/processUserInput/processUserInput.ts#L6)
- [processUserInput/processTextPrompt.ts](file:///workspace/caiode/claude-code-src/utils/processUserInput/processTextPrompt.ts#L1)
- [processUserInput/processSlashCommand.tsx](file:///workspace/caiode/claude-code-src/utils/processUserInput/processSlashCommand.tsx#L2)
- [processUserInput/processBashCommand.tsx](file:///workspace/caiode/claude-code-src/utils/processUserInput/processBashCommand.tsx#L1)

**核心功能**:
- Claude API 客户端
- 消息处理
- 工具定义
- 内容块管理

**package.json 声明**: ✅ 已声明

---

## 架构影响分析

### 沙箱系统架构

```
┌─────────────────────────────────────────────────────────┐
│              Claude Code Sandbox System                  │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────────┐     ┌──────────────────────┐    │
│  │ SandboxManager   │────▶│ BaseSandboxManager   │    │
│  │ (Claude 适配器)  │     │ (@anthropic-ai/     │    │
│  └──────────────────┘     │ sandbox-runtime)     │    │
│           ▲               └──────────────────────┘    │
│           │                                              │
│  ┌────────┴────────┐                                     │
│  │ 配置转换器       │                                     │
│  │ (convertTo...   │                                     │
│  │  Sandbox...)    │                                     │
│  └─────────────────┘                                     │
│                                                           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌───────────────────┐
              │  替代方案         │
              ├───────────────────┤
              │ ✅ Git Worktree  │
              │ ✅ InProcess     │
              │ ✅ Tmux 隔离     │
              └───────────────────┘
```

---

## 迁移建议

### 优先级 P0 - 立即处理

1. **沙箱系统替代**
   - 使用 Git Worktree + 钩子系统替代沙箱隔离
   - 工作原理已在 [沙箱替代方案评估.md](file:///workspace/docs/planning/沙箱替代方案评估.md) 中详细说明

2. **MCPB 包确认**
   - 确认 `@anthropic-ai/mcpb` 是否可以开源或替换
   - 评估是否可以使用标准 MCP SDK 替代

---

### 优先级 P1 - 短期处理

1. **依赖声明**
   - 在 package.json 中明确声明所有使用的依赖
   - 或移除对未声明包的引用

2. **代码重构**
   - 将沙箱相关代码重构为可插拔架构
   - 支持多种隔离后端（沙箱、worktree、in-process）

---

## 代码参考清单

### 沙箱相关文件
- [sandbox-adapter.ts](file:///workspace/caiode/claude-code-src/utils/sandbox/sandbox-adapter.ts) - 沙箱适配器（985 行）
- [worktree.ts](file:///workspace/caiode/claude-code-src/utils/worktree.ts) - Git Worktree 实现（1200+ 行）
- [hooks.ts](file:///workspace/caiode/claude-code-src/utils/hooks.ts) - 钩子系统

### MCP 相关文件
- [plugins/mcpbHandler.ts](file:///workspace/caiode/claude-code-src/utils/plugins/mcpbHandler.ts) - MCPB 处理器
- [dxt/helpers.ts](file:///workspace/caiode/claude-code-src/utils/dxt/helpers.ts) - DXT 辅助工具

### SDK 相关文件
- [tokens.ts](file:///workspace/caiode/claude-code-src/utils/tokens.ts) - 令牌管理
- [toolSchemaCache.ts](file:///workspace/caiode/claude-code-src/utils/toolSchemaCache.ts) - 工具模式缓存
- [sideQuery.ts](file:///workspace/caiode/claude-code-src/utils/sideQuery.ts) - 侧边查询

---

## 总结

| 指标 | 数值 |
|------|------|
| 发现的非开源包 | 2 个 |
| 已声明的开源包 | 1 个 |
| 受影响代码行数 | ~1,428 行 |
| 已有替代方案 | ✅ 是（Git Worktree） |
| 需要立即迁移 | ✅ 是 |

**关键发现**:
1. `@anthropic-ai/sandbox-runtime` 是核心非开源依赖，但项目已有完整的 Git Worktree 替代方案
2. `@anthropic-ai/mcpb` 需要进一步确认其开源状态
3. `@anthropic-ai/sdk` 已开源，无需替换
4. 代码架构良好，支持多种隔离后端的插拔式设计

---

**报告生成完成**
